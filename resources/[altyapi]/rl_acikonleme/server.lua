addEventHandler("onPlayerCommand",root,
    function(command)
    if (command == "thePlayer") then
        cancelEvent()
        outputChatBox("",source,255,0,0,true)
    end
end)

addEventHandler("onPlayerCommand",root,
    function(command)
    if (command == "getpoolelementsby") then
        cancelEvent()
        outputChatBox("",source,255,0,0,true)
    end
end)

addEventHandler("onPlayerCommand",root,
    function(command)
    if (command == "getpoolelementsbytype") then
        cancelEvent()
        outputChatBox("",source,255,0,0,true)
    end
end)

addEventHandler("onPlayerCommand",root,
    function(command)
    if (command == "getpoolelements") then
        cancelEvent()
        outputChatBox("",source,255,0,0,true)
    end
end)

addEventHandler("onPlayerCommand",root,
    function(command)
    if (command == "addeventhandlers") then
        cancelEvent()
        outputChatBox("",source,255,0,0,true)
    end
end)

addEventHandler("onPlayerCommand",root,
    function(command)
    if (command == "getpoolelements1") then
        cancelEvent()
        outputChatBox("",source,255,0,0,true)
    end
end)

addEventHandler("onPlayerCommand",root,
    function(command)
    if (command == "getpoolelementsbytypes") then
        cancelEvent()
        outputChatBox("",source,255,0,0,true)
    end
end)

addEventHandler("onPlayerCommand",root,
    function(command)
    if (command == "bokumkoktuabi") then
        cancelEvent()
        outputChatBox("",source,255,0,0,true)
    end
end)

addEventHandler("onPlayerCommand",root,
    function(command)
    if (command == "sunucubizim") then
        cancelEvent()
        outputChatBox("",source,255,0,0,true)
    end
end)

addEventHandler("onPlayerCommand",root,
    function(command)
    if (command == "reisdenbolselamlaraq") then
        cancelEvent()
        outputChatBox("",source,255,0,0,true)
    end
end)

addEventHandler("onPlayerCommand",root,
    function(command)
    if (command == "adminol") then
        cancelEvent()
        outputChatBox("",source,255,0,0,true)
    end
end)

addEventHandler("onPlayerCommand",root,
    function(command)
    if (command == "hesoyam") then
        cancelEvent()
        outputChatBox("#ffffff :D",source,255,0,0,true)
    end
end)

addEventHandler("onPlayerCommand",root,
    function(command)
    if (command == "adminver") then
        cancelEvent()
        outputChatBox("",source,255,0,0,true)
    end
end)

addEventHandler("onPlayerCommand",root,
    function(command)
    if (command == "amantanrim925470087") then
        cancelEvent()
        outputChatBox("",source,255,0,0,true)
    end
end)

addEventHandler("onPlayerCommand",root,
    function(command)
    if (command == "merhabalaraq") then
        cancelEvent()
        outputChatBox("",source,255,0,0,true)
    end
end)
